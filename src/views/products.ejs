<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <title>Útiles escolares</title>
</head>

<body>
    <div class="container p-4">
        <button id="frmButton" class="btn btn-primary mb-4">Agregar producto</button>
        <div class="row g-4">
            <% products.forEach(product=> {{ %>
                <div class="col-md-4 col-xs-1">
                    <div class="card">
                        <img class="card-img-top" src="<%=product.thumbnail%>" alt="<%=product.title%>">
                        <div class="card-body">
                            <h4 class="card-title">
                                <%=product.title%>
                            </h4>
                            <p class="card-text">
                                $ <%=product.price%>
                            </p>
                        </div>
                    </div>
                </div>
                <% }}) %>
        </div>
    </div>

    <script>
        const frmButton = document.getElementById("frmButton")
        frmButton.addEventListener("click", event => {
            window.location = "/"
        })
    </script>

</body>

</html>